import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.tonystark.xyz.counter.app',
    appName: 'Counter with Metrics',
    webDir: 'build',
};

export default config;
